<form method="post" action="{{ path('app_pickup_appointment_delete', {'id': pickup_appointment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pickup_appointment.id) }}">
    <button class="btn">Delete</button>
</form>
